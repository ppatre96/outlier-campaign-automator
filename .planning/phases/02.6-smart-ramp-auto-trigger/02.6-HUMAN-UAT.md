---
status: partial
phase: 02.6-smart-ramp-auto-trigger
source: [02.6-VERIFICATION.md]
started: 2026-04-27T00:00:00Z
updated: 2026-04-27T00:00:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. Install the launchd plist
expected: `~/Library/LaunchAgents/com.outlier.smart-ramp-poller.plist` exists and points at `/Users/pranavpatre/outlier-campaign-agent/scripts/smart_ramp_poller.py` with `StartInterval=900` and `RunAtLoad=true`. The exact plist XML is documented in `README.md` "Smart Ramp Poller (Phase 2.6)" section.

Steps:
1. Copy the plist XML from `README.md` into the plist file
2. Validate: `plutil -lint ~/Library/LaunchAgents/com.outlier.smart-ramp-poller.plist`
3. Load: `launchctl unload ~/Library/LaunchAgents/com.outlier.smart-ramp-poller.plist 2>/dev/null; launchctl load ~/Library/LaunchAgents/com.outlier.smart-ramp-poller.plist`
4. Verify loaded: `launchctl list | grep com.outlier.smart-ramp-poller` (should return one line with `0` exit code)

result: [pending]

### 2. Invite the bot to channel `C0B0NBB986L`
expected: The Slack bot identified by `SLACK_BOT_TOKEN` is a member of channel `C0B0NBB986L`. Without this, channel posts return `not_in_channel` and only the two solo DMs land — notifier handles gracefully but the channel announcement is silently lost.

Steps:
1. In Slack, navigate to channel `C0B0NBB986L`
2. Type: `/invite @<bot_name>` (replace `<bot_name>` with the actual bot — check `SLACK_BOT_TOKEN` owner)
3. Verify: send a test message via `venv/bin/python -c "from dotenv import load_dotenv; load_dotenv(); from slack_sdk import WebClient; import config; WebClient(token=config.SLACK_BOT_TOKEN).chat_postMessage(channel='C0B0NBB986L', text='smart-ramp-poller bot membership test')"` — should return `ok: True`

result: [pending]

### 3. Optional — Diego sends one DM to the bot
expected: Diego's user ID `U08AW9FCP27` has an open IM channel with the bot. Without it, `conversations.open` may return `cannot_dm_bot` on the first poll. Notifier handles this gracefully (logs warning, continues), but the Diego DM is silently lost on Diego's first ramp.

Steps:
1. Ask Diego to DM the bot any message (one character is enough)
2. The bot's IM channel for Diego is now open

result: [pending — recommended but optional]

### 4. Smoke-test the new poller before letting cron loose
expected: A manual `--once --dry-run` run completes without error and confirms the wired pipeline + notifier work end-to-end.

Steps:
```bash
cd /Users/pranavpatre/outlier-campaign-agent
venv/bin/python scripts/smart_ramp_poller.py --once --ramp-id GMR-0010 --dry-run
```
Confirm:
- All 8 pre-seeded ramps classify as `action=noop` on first live poll (signature matches the seed)
- `--ramp-id GMR-0010` forces processing of just that one ramp
- `--dry-run` mode does NOT post to Slack and does NOT call `outlier-data-analyst` reanalysis
- The dry-run preview is logged to stdout
- `data/processed_ramps.json` is NOT updated (dry-run does not move state)

result: [pending]

### 5. Pre-existing v1 bug surfaced during verification (out of scope, tracking only)
expected: `python main.py --ramp-id GMR-0010 --dry-run` should still work for ad-hoc manual runs.

Status: KNOWN BROKEN — `AttributeError: 'RedashClient' object has no attribute 'fetch_stage1_contributors'`. This is a pre-existing v1 path bug — `_process_row` was NOT modified by Phase 2.6 (verified by reading commit 158f5a8). Phase 2.6's new `--modes` path uses `run_launch_for_ramp` instead and is unaffected.

Steps:
- Track separately. File a bug or address in a future polish phase. Does NOT block Phase 2.6 closure.

result: [tracked separately — out of scope]

### 6. First live cron fire (after steps 1+2 above)
expected: Within 15 minutes of `launchctl load`, the first poll fires; logs to `logs/smart_ramp_poller/$(date -u +%Y-%m-%d).log`. All 8 pre-seeded ramps classify as `action=noop` and no Slack messages are sent.

Steps:
1. After step 1 + 2 are done, wait up to 15 min
2. `tail -f logs/smart_ramp_poller/$(date -u +%Y-%m-%d).log`
3. Confirm a "previous poll" or "first poll" log line appears
4. Confirm "8 ramps classified as noop" or equivalent line
5. Confirm zero Slack messages posted (no DMs, no channel posts)

result: [pending — depends on steps 1+2 above]

## Summary

total: 6
passed: 0
issues: 0
pending: 6
skipped: 0
blocked: 0
notes: |
  Steps 1+2 are blocking for FEED... err SR-09 to go live.
  Steps 3+4+6 are recommended verification.
  Step 5 is out-of-scope — pre-existing v1 path bug; tracked here only for visibility.

## Gaps

(none — these are operational handoffs, not code gaps. The pre-existing
`RedashClient.fetch_stage1_contributors` bug is documented in step 5 as
out-of-scope tracking; it does not regress anything Phase 2.6 added.)
