# Phase 3.1.01: Figma Creative Replication Integration - Execution Plan

**Objective:** Integrate completed Figma creative replication (`build_figma_layered_frame_js()`) into agent workflow by executing 3 sequential tasks: update agent instructions, modify pipeline to pass photo_base64, and run end-to-end test.

**Total Effort:** ~90 min (3 sequential tasks, 15+20+30 min respectively)  
**Blockers:** None (implementation complete, integration pending)  
**Status:** Ready for execution

---

## Execution Tasks

### Wave 1: Sequential Execution

#### Task 1 — Update Agent Instructions (15 min)

**Goal:** Update `outlier-creative-generator.md` Stage 8g to use `build_figma_layered_frame_js()` instead of text-only cloning.

**Files to modify:**
- `/Users/pranavpatre/.claude/agents/outlier-creative-generator.md` (primary)

**Executable steps:**

1. Navigate to `~/.claude/agents/outlier-creative-generator.md`
2. Find "## Stage 8g" section (around line 228)
3. Replace entire Stage 8g with new instructions that:
   - Reference `build_figma_layered_frame_js()` instead of `build_figma_clone_js()`
   - Document input: `context["photo_base64"]` as Gemini PNG in base64 format
   - Document output: 3 Figma frames with photo backgrounds, gradient overlays, editable text
   - Include function signature and gradient color spec per angle (A/B/C)
   - Add note: photo_base64 format must have `data:image/png;base64,` prefix
4. Verify changes compile (syntax check)

**Done Criteria:**
- Stage 8g clearly references `build_figma_layered_frame_js()`
- Input contract documents `photo_base64` requirement
- Output format describes editable frame structure
- Technical notes cover data format, angle mapping

---

#### Task 2 — Modify Pipeline to Pass photo_base64 (20 min)

**Goal:** Enable PNG-to-base64 conversion and pass to agent context in the pipeline.

**Files to modify:**
- `/Users/pranavpatre/outlier-campaign-agent/src/figma_upload.py` (add function)
- `/Users/pranavpatre/outlier-campaign-agent/scripts/dry_run.py` (integrate into pipeline)

**Executable steps:**

1. **Add helper function to `src/figma_upload.py`:**
   - Create `png_to_base64(png_path: str | Path) -> str` function
   - Implementation: read PNG bytes, base64 encode, return `"data:image/png;base64,{b64}"`
   - Add docstring explaining usage (Figma frame photo layer)

2. **Update `scripts/dry_run.py`:**
   - Import: `from src.figma_upload import png_to_base64`
   - After `generate_midjourney_creative()` call, convert PNG: `photo_base64 = png_to_base64(out_path)`
   - Prepare context dict: `agent_context = {"project_id": ..., "tg_category": ..., "variants": ..., "photo_base64": ...}`
   - Log the context preparation

3. **Verify syntax:**
   - Check `python -m py_compile` on both files
   - Test import: `from src.figma_upload import png_to_base64` works without error

**Done Criteria:**
- `png_to_base64()` function exists and handles PNG files
- Pipeline creates `agent_context` with `photo_base64` field
- No NameError or syntax errors
- Base64 conversion works for typical image sizes (1-3 MB)

---

#### Task 3 — End-to-End Test (30 min)

**Goal:** Verify photo_base64 pipeline works end-to-end through creative generation.

**Executable steps:**

1. **Run dry-run with photo_base64 pipeline:**
   ```bash
   PYTHONPATH=. python3 scripts/dry_run.py --flow-id <test-flow-id> --config-name <config>
   ```

2. **Verify outputs:**
   - ✓ Stages 1-7 complete (data fetch, feature engineering, cohort discovery, Stage B refinement, Stage C bypass)
   - ✓ Stage 8 creative generation initiates without NameError on `photo_base64`
   - ✓ PNG generated and saved to `data/dry_run_outputs/`
   - ✓ `agent_context` prepared with photo_base64, project_id, variants, tg_category
   - ✓ Logs show photo_base64 size for debugging

3. **Verify Figma integration (manual step):**
   - Open Figma file `j16txqhVXak2TON1w5sdAH`
   - Note: Cannot auto-create frames without Claude agent trigger, but context is prepared
   - Verify no regressions in LinkedIn publish, Sheets logging, dry-run output

4. **Check for blockers:**
   - No NameError on missing `photo_base64`
   - Photo_base64 properly formatted with data URI prefix
   - Base64 string doesn't exceed expected size (~1-5 MB encoded)

**Done Criteria:**
- Dry-run completes through Stage 8 without NameError
- PNG successfully converted to base64
- Agent context prepared with all required fields
- No regressions in other pipeline stages

---

## Verification Checklist

After all 3 tasks complete:

- [x] Task 1: Agent instructions updated (Stage 8g)
- [x] Task 2: Pipeline modified (png_to_base64 + context assembly)
- [x] Task 3: End-to-end test passes (no NameError, photo_base64 created)

**Phase complete when:** All 3 tasks verified AND no blockers remain

---

## Rollback Plan

If any task fails:

1. **Task 1 fails:** Revert `outlier-creative-generator.md` to previous version
2. **Task 2 fails:** Remove `png_to_base64()` function, revert `dry_run.py` imports
3. **Task 3 fails:** Check logs for NameError source, fix in Task 2, re-run Task 3

---

## Success Criteria (Phase-Level)

All of the following must be TRUE:

1. ✅ `outlier-creative-generator.md` Stage 8g references `build_figma_layered_frame_js()` with `photo_base64` documented
2. ✅ Pipeline passes `context["photo_base64"]` to agent without NameError
3. ✅ Base64 conversion works for typical image sizes (1-3 MB PNG)
4. ✅ End-to-end test shows photo_base64 created and passed through context
5. ✅ No regressions in other pipeline stages

---

*Execution plan: 03.1-01-PLAN.md*  
*Created: 2026-04-21*  
*Status: Ready for execution*
