# Continue Here — Phase 1: Pipeline Integrity

**Paused:** 2026-04-21  
**Last commit:** `72b2884 docs(phase-2): research observability & storage phase`

---

## Where We Are

Phase 1 is **in progress**. Plans 01-01 and 01-02 are complete. Plans 01-03 and 01-04 are written and ready to execute.

Phase 2 (Observability & Storage) is fully planned — all 4 plans written and verified READY TO EXECUTE.

---

## What Was Done This Session

### Figma Creative — SOLVED
- `figma.createImage()` is broken for any PNG > ~100 bytes in the Plugin API MCP sandbox (renders black)
- Switched to **native Figma elements** approach: Frame + gradient rectangle fills + text nodes via `use_figma` MCP
- Created and confirmed working: node `20:2` in file `j16txqhVXak2TON1w5sdAH`
  - Frame: `69cf1a039ed66cc82e0fa8f3_A_v1` (1200×1200)
  - Headline: "Between cardiac cases? AI needs your expertise."
  - Subheadline: "Review AI responses using your clinical knowledge."
  - Bottom strip: "Earn $25–$50 USD per hour." + "Fully remote." + "Outlier" wordmark (brown)
- Updated `src/figma_upload.py` with `get_figma_frame_name()` helper and warning docstring
- Updated `~/.claude/agents/campaign-manager.md` with Stage 8g (native elements spec)

### Sub-Agent Architecture — DOCUMENTED
- Confirmed: script path (`dry_run.py`/`main.py`) uses NO Claude sub-agents — direct Python API calls only
- Confirmed: Claude Code interactive path spawns `outlier-data-analyst`, `ad-creative-brief-generator`, `outlier-copy-writer`, `outlier-creative-generator`
- `campaign-manager` is the orchestrator — supervises all stages, holds shared context
- `competitor-bot` runs weekly independently, writes `data/competitor_intel/latest.json`
- `ad-creative-brief-generator` reads `latest.json` at Stage 8b to incorporate experiment ideas
- Wrote `AGENT-PIPELINE.md` (project root) — full architecture reference
- Wrote `01-04-PLAN.md` — sub-agent trace logging + context validation

### Phase 2 — FULLY PLANNED
- `02-01-PLAN.md`: Slack Bot integration (slack-sdk WebClient, crontab)
- `02-02-PLAN.md`: Fix drive_url drop at main.py line 367, extend write_creative()
- `02-03-PLAN.md`: Add validate_photo_subject() guard in midjourney_creative.py
- `02-04-PLAN.md`: Wire campaign monitor summary into weekly Slack report (depends on 02-01)
- `VERIFICATION.md`: All 4 plans PASS — 0 blockers

---

## Next Steps (in order)

### 1. Execute Plan 01-03 (LinkedIn token refresh + full dry-run acceptance)
```
/gsd:execute-phase 1 --wave 2
```
Plan 01-03 verifies:
- Manual `refresh_access_token()` succeeds → new token written to .env
- Full dry-run `scripts/dry_run.py --project-id 698a172324c01532c2f92a0d --skip-creatives` completes end-to-end
- All 5 stage outputs appear in stdout

### 2. Execute Plan 01-04 (Sub-agent trace logging)
After 01-03 passes:
```
/gsd:execute-phase 1 --wave 3
```
Plan 01-04 adds:
- `[Agent 8b/8c/8d]` print statements in `scripts/dry_run.py` Stage 8 loop
- Signal count logging in `src/figma_creative.py` `build_copy_variants()`
- photo_subject + prompt logging in `src/midjourney_creative.py` `generate_midjourney_creative()`
- competitor intel JSON writer in `scripts/competitor_intel.py`
- competitor intel loader in `src/figma_creative.py` `build_copy_variants()`

### 3. Execute Phase 2
```
/gsd:execute-phase 2
```
Note: 02-01 (Slack) requires human action first — Slack App creation.
02-04 depends on 02-01 completing.

---

## Human Actions Required Before Continuing

| Action | Blocks |
|--------|--------|
| Create Slack App with `chat:write` scope, install to workspace, set `SLACK_BOT_TOKEN` in `.env` | 02-01, 02-04 |
| Create Google Shared Drive, add service account as Content Manager, set `GDRIVE_FOLDER_ID` | 02-02 |
| Request LinkedIn `r_liteprofile` scope, re-auth, call `GET /v2/me` for `LINKEDIN_MEMBER_URN` | LI-01 (Phase 1) |
| LinkedIn MDP approval for audienceCounts (account 510956407) | Stage C (graceful bypass already in place) |

---

## Key File Locations

| File | Purpose |
|------|---------|
| `AGENT-PIPELINE.md` | Full sub-agent architecture reference (both execution paths) |
| `src/figma_upload.py` | Figma frame helpers (get_figma_frame_name, prepare_for_figma) |
| `~/.claude/agents/campaign-manager.md` | Campaign orchestrator prompt — includes Stage 8g (Figma native) |
| `.planning/phases/02-observability-storage/` | All Phase 2 plans ready to execute |
| `data/competitor_intel/latest.json` | Competitor intel (populated by competitor-bot weekly run) |

---

## Figma Spec (for next creative upload)

File key: `j16txqhVXak2TON1w5sdAH`  
Frame naming: `{project_id}_{angle}_v{version}` (e.g. `69cf1a039ed66cc82e0fa8f3_A_v1`)  
Layout: 1200×1200, border=40, strip_h=168, gradient bg, Inter Bold headline, Inter Regular subheadline, brown Outlier wordmark

**DO NOT use `figma.createImage()` for PNG uploads — renders black for images > ~100 bytes.**  
Use native Figma elements via `use_figma` MCP instead.
