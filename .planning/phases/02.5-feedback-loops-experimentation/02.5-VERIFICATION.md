---
status: human_needed
phase: 02.5-feedback-loops-experimentation (V2 extension)
goal_summary: Close the optimization loop end-to-end via full-funnel tracking, sentiment mining, ICP drift detection, and a single weekly cron orchestrator.
verified_at: 2026-04-25T05:10:00Z
---

## Goal Achievement

The V2 extension achieved its goal **in code** across all four plans. Funnel decomposition (FEED-15/16) is wired through `FeedbackAgent`, sentiment mining (FEED-17/18/19) ships in `src/sentiment_miner.py` with Apple/Play deliberately excluded per RESEARCH-V2, ICP drift detection (FEED-20/21) uses `scipy.stats.entropy` with a 7-day rate limit, and the weekly cron orchestrator (FEED-22/23) wires all four signals into one consolidated Slack post with filelock idempotency, --dry-run / --force / --only flags, and step isolation. All 48 tests pass (5 v1 feedback_agent + 3 V2 funnel + 5 sentiment_miner + 5 icp_drift_monitor + 5 weekly_feedback_loop + 25 from earlier phases).

The single outstanding gap is **operational, not code-level**: FEED-22 requires a one-time launchd plist edit + crontab dedup which the agent was explicitly forbidden from executing per Plan 08 critical_constraints. Both actions are documented in `README.md` and the SUMMARY's "USER ACTION REQUIRED" block. Until the user runs them, Monday cron still fires the v1 `post_weekly_reports.py` AND there is no entry yet for the new orchestrator.

## Requirement Coverage

| ID | Status | Plan | Evidence |
|----|--------|------|----------|
| FEED-15 | ✓ | 02.5-05 | `FeedbackAgent.analyze_funnel_by_cohort` + `RedashClient.query_funnel_metrics` + `FUNNEL_METRICS_SQL` (LEFT JOIN PUBLIC.GROWTHRESUMESCREENINGRESULTS, UPPER(g.RESULT)='PASS'). Test `test_analyze_funnel_by_cohort_returns_stage_rates` passes. |
| FEED-16 | ✓ | 02.5-05 | `FeedbackAgent.identify_funnel_drop_stage` (earliest-stage-wins, cohort-median baseline, FUNNEL_DROP_ALERT_THRESHOLD=0.30) + `generate_slack_alert(funnel_diagnosis=None)` backwards-compatible kwarg. Tests `test_identify_funnel_drop_stage_classifies_earliest_drop` + `test_alert_includes_drop_stage` pass. |
| FEED-17 | ✓ | 02.5-06 | `src/sentiment_miner.py` exports `fetch_reddit`, `fetch_trustpilot`, `fetch_glassdoor`, `fetch_discourse`. User-Agent literal `outlier-campaign-agent/0.2 (sentiment_miner; +https://outlier.ai)` present (Pitfall 3 fix). `test_fetcher_isolation` confirms one-source-failure does not block others. NO `itunes.apple` / `play.google` references — verified. |
| FEED-18 | ✓ | 02.5-06 | `fetch_zendesk` (HTTP Basic `{email}/token:{token}`) + `fetch_intercom` (Bearer). Both gracefully return `[]` + log warning when creds empty. Tests `test_zendesk_skip_no_creds` + `test_intercom_skip_no_creds` pass. PII rule: 800-char body truncation in-memory, 400-char in raw dump; no requester_id / name / email persisted. |
| FEED-19 | ✓ | 02.5-06 | `data/sentiment_callouts.json` schema = `{generated_at, sources_queried, sources_skipped, themes[]}`. Themes < SENTIMENT_THEME_MIN_EVIDENCE (=3) land in `data/sentiment_raw/<date>.json`. `_VOCAB_SCRUB` regex post-processes theme labels. Tests `test_evidence_threshold` + `test_no_banned_vocab` pass. `.claude/agents/ad-creative-brief-generator.md` extended with "Sentiment-Driven Copy Inputs" section honoring "Address:" directives. |
| FEED-20 | ✓ | 02.5-07 | `src/icp_drift_monitor.py:29 from scipy.stats import entropy` + `EPSILON = 1e-10` (Pitfall 4 fix). No hand-rolled KL math (grep `np.log(p/q)` returns 0). `compute_drift` returns `{drift_score, per_feature, cold_start, n_rows}`. Cold-start (<2 snapshots) returns `cold_start=True` + `drift_score=None`. Tests `test_kl_synthetic_shift` + `test_cold_start` pass. |
| FEED-21 | ✓ | 02.5-07 | `check_and_trigger` invokes `ReanalysisOrchestrator().trigger_reanalysis(reason="icp_drift")` IFF drift > threshold AND n_rows >= ICP_DRIFT_MIN_ROWS (=200) AND no trigger in past 7 days. State persisted in `data/icp_drift_state.json`. Tests `test_auto_trigger_on_drift` + `test_rate_limit_7d` + boundary test `test_rate_limit_boundary_at_7_days` pass. `_invoke_trigger` handles both sync and async orchestrator signatures. |
| FEED-22 | ⚠ HUMAN_NEEDED | 02.5-08 | **Code complete.** `scripts/weekly_feedback_loop.py` (546 LOC) with `main()`, `run_once()`, `--dry-run` / `--force` / `--only`, `filelock.FileLock(timeout=10)`, 6-day SKIP_WINDOW, step isolation. Tests `test_idempotency` + `test_dry_run` + `test_step_isolation` pass. **Operational gap:** crontab still has `30 3 * * 1 ... post_weekly_reports.py` (verified live: `crontab -l` shows duplicate); launchd plist `~/Library/LaunchAgents/com.outlier.weekly-reports.plist` (mtime Apr 20, predates this phase) still points at v1 script. User must run the four documented commands before FEED-22 is fully live. |
| FEED-23 | ✓ | 02.5-08 | `_build_consolidated_message` produces four-section Slack post in correct order: Creative Progress Alerts -> Funnel Drop Diagnosis -> Sentiment Themes -> ICP Drift. Reuses v1 `_post_to_slack` (no reimplementation). Tests `test_consolidated_slack` + `test_slack_vocabulary` pass. |

## v1 Backward Compatibility

v1 contracts are intact. `FeedbackAgent.analyze_creative_performance` and `FeedbackAgent.identify_underperforming_cohorts` still exist with their original v1 signatures (`venv/bin/python -c "from src.feedback_agent import FeedbackAgent; assert hasattr(FeedbackAgent, ...)"` passes). `FeedbackAgent.generate_slack_alert` is extended with the optional `funnel_diagnosis: dict | None = None` kwarg — defaults to `None` so all v1 two-arg callers (including `scripts/post_weekly_reports.post_weekly_feedback_alert` which the V2 orchestrator wraps) continue to work unchanged. `scripts.post_weekly_reports.post_weekly_feedback_alert` and `scripts.post_weekly_reports._post_to_slack` are both still importable and callable.

All 5 v1 tests in `tests/test_feedback_agent.py::TestFeedbackAgent` (test_analyze_creative_performance_returns_list, test_identify_underperforming_cohorts_detects_z_score, test_generate_slack_alert_formats_text, test_empty_query_returns_empty_list, test_hypothesis_generation_logic) still pass alongside the 3 new V2 funnel tests — 8/8 in the file.

## Test Suite

```
$ venv/bin/python -m pytest tests/ -q --tb=line --ignore=tests/test_phase_31_figma_integration.py
................................................                         [100%]
48 passed, 1 warning in 1.38s
```

Single warning is the pre-existing `datetime.utcnow()` deprecation in `config.py:32` — out of scope for Phase 2.5 V2 (predates this phase).

Breakdown:
- 8 in `test_feedback_agent.py` (5 v1 + 3 V2)
- 5 in `test_sentiment_miner.py`
- 5 in `test_icp_drift_monitor.py` (4 required + 1 boundary)
- 5 in `test_weekly_feedback_loop.py`
- 25 from earlier phases (LinkedIn URN, ICP, prompt-style, etc.)

The `test_phase_31_figma_integration.py` test is excluded by `pytest.ini` because it requires the Figma plugin server.

## V2 Must-Haves Spot Checks

1. **`from scipy.stats import entropy` in `src/icp_drift_monitor.py`** — PASS (line 29 grep confirmed).
2. **`EPSILON = 1e-10` in `src/icp_drift_monitor.py`** — PASS (line 35 grep confirmed; Pitfall 4 fix).
3. **No hand-rolled KL math (`np.log(p/q)` patterns) in `src/icp_drift_monitor.py`** — PASS (grep returns 0 matches).
4. **`src/sentiment_miner.py` does NOT contain `itunes.apple` or `play.google` URL patterns** — PASS (grep returns 0 matches; Apple/Play deliberately excluded per RESEARCH-V2 finding that Outlier has no native mobile app).
5. **`scripts/weekly_feedback_loop.py` imports `filelock`** — PASS (line 53: `from filelock import FileLock, Timeout`).
6. **`scripts/weekly_feedback_loop.py` has `--dry-run`, `--force`, `--only` CLI flags** — PASS (`--help` output shows all three).
7. **`_post_to_slack` helper from v1 is reused (not reimplemented)** — PASS (line 466-468: `from scripts.post_weekly_reports import _post_to_slack`; no `chat.postMessage` or webhook URL re-implementation in `weekly_feedback_loop.py`).
8. **`requirements.txt` has `filelock`, `pyarrow`, `scipy`** — PASS (each pinned exactly once).
9. **`SENTIMENT_THEME_MIN_EVIDENCE=3`, `ICP_DRIFT_THRESHOLD=0.15`, `ICP_DRIFT_MIN_ROWS=200`, `FUNNEL_DROP_ALERT_THRESHOLD=0.30`** — PASS (runtime probe confirms each default).
10. **`ad-creative-brief-generator.md` has "Sentiment-Driven Copy Inputs" section** — PASS.
11. **All 8 sentiment_miner public symbols exist (`run`, `fetch_reddit`, ..., `extract_themes`)** — PASS.
12. **All 4 icp_drift_monitor public symbols exist (`snapshot`, `compute_drift`, `check_and_trigger`, `categorical_kl`)** — PASS.
13. **All Plan 08 callables exist (`main`, `run_once`, `_build_consolidated_message`, `_should_skip`)** — PASS.

## Cross-Plan Integration

Plan 08 wires Plans 05/06/07 correctly:

| Step | Plan 08 call | Target | Verified |
|------|--------------|--------|----------|
| B    | `agent.analyze_funnel_by_cohort(days_back=7)` (line 201) | Plan 05 | `FeedbackAgent.analyze_funnel_by_cohort` exists with matching signature ✓ |
| B    | `agent.identify_funnel_drop_stage(rows)` (line 202) | Plan 05 | `FeedbackAgent.identify_funnel_drop_stage` exists ✓ |
| C    | `sentiment_miner.run()` (line 215) | Plan 06 | `sentiment_miner.run()` exists with matching return shape ✓ |
| D    | `idm.check_and_trigger(pid)` (line 248, live mode) | Plan 07 | `icp_drift_monitor.check_and_trigger(project_id)` exists with matching contract ✓ |
| D    | `idm.snapshot(pid)` + `idm.compute_drift(pid)` (dry-run mode) | Plan 07 | both exist; dry-run correctly bypasses `check_and_trigger` to prevent outlier-data-analyst invocation ✓ |
| A    | `post_weekly_reports.main()` in-process (line 178) | v1 | v1 callable preserved ✓ |
| E    | `_post_to_slack(msg)` (line 468) | v1 | v1 helper reused, NOT reimplemented ✓ |

All four cross-plan API call sites match the public APIs the upstream plans exported. No stub or signature mismatch.

## Vocabulary Compliance

Production source paths scanned for CLAUDE.md banned tokens (excluding comments, NEVER rules in LLM prompts, and the `_VOCAB_SCRUB` regex banlist):

| File | `compensation` | `interview` | `project rate` | `bonus` | `assign` | Status |
|------|----------------|-------------|----------------|---------|----------|--------|
| `scripts/weekly_feedback_loop.py` | 0 | 0 | 0 | 0 | 0 | ✓ |
| `src/feedback_agent.py` | 0 | 1 (NEVER rule line 28) | 0 | 0 | 0 | ✓ |
| `src/sentiment_miner.py` | inside `_VOCAB_SCRUB` table + LLM "NEVER" prompt | inside same | inside same | inside same | inside same | ✓ |
| `src/icp_drift_monitor.py` | 0 | 0 | 0 | 0 | 0 | ✓ |

The "discourse" token appears in `src/sentiment_miner.py` log messages (e.g. `log.info("discourse: fetched %d topics", ...)`) — these are internal log lines, NOT user-facing Slack output. The Outlier-banned context for "Discourse" is when referring to the forum platform name in user-facing copy; here it's a source-name identifier in internal logs. Acceptable.

The `_build_consolidated_message` Slack output uses approved terms: "Creative Progress Alerts" (NOT "Performance"), "Funnel Drop Diagnosis", "Sentiment Themes", "ICP Drift", "rate" / "baseline" / "delta". The unit test `test_slack_vocabulary` runs a regex banlist over a synthesized message and passes.

## Outstanding Issues / Human-Needed Items

### 1. Update launchd plist to point at the new orchestrator

**Test:** Run the following commands to repoint the live Monday cron from v1 to the V2 orchestrator:

```bash
# 1. Edit the plist (open in TextEdit and change the ProgramArguments path)
open -e ~/Library/LaunchAgents/com.outlier.weekly-reports.plist

# Change this line:
#   <string>/Users/pranavpatre/outlier-campaign-agent/scripts/post_weekly_reports.py</string>
# To:
#   <string>/Users/pranavpatre/outlier-campaign-agent/scripts/weekly_feedback_loop.py</string>

# 2. Reload launchd
launchctl unload ~/Library/LaunchAgents/com.outlier.weekly-reports.plist
launchctl load   ~/Library/LaunchAgents/com.outlier.weekly-reports.plist

# 3. Verify
grep weekly_feedback_loop.py ~/Library/LaunchAgents/com.outlier.weekly-reports.plist  # should match
launchctl list | grep com.outlier.weekly                                              # shows loaded job
```

**Expected:** plist file mtime updates, `launchctl list` shows the label loaded with PID=0 status=0, and `grep` returns a match.

**Why human:** Plan 08's `<critical_constraints>` explicitly forbade the agent from editing user system files (`~/Library/LaunchAgents/`). Misedited plists can break the user's auto-launch infrastructure and require manual recovery.

**Verified live state (2026-04-25):** plist file unchanged since `Apr 20 09:53` (mtime predates this phase's commits). User has not yet run the migration commands.

### 2. Remove the duplicate crontab entry

**Test:**

```bash
crontab -l | grep -v post_weekly_reports | crontab -
crontab -l | grep post_weekly_reports                  # must return empty
```

**Expected:** `crontab -l | grep post_weekly_reports` exits 1 (empty output), confirming the duplicate Monday cron is removed.

**Why human:** Same `<critical_constraints>` reason — Plan 08 forbade the agent from running `crontab -e` or `crontab -`. Without removing this duplicate, two Monday posts will fire (Pitfall 1: double-cron bug).

**Verified live state (2026-04-25):**

```
30 3 * * 1 /Users/pranavpatre/outlier-campaign-agent/venv/bin/python /Users/pranavpatre/outlier-campaign-agent/scripts/post_weekly_reports.py >> /tmp/outlier_weekly_report.log 2>&1
```

The duplicate crontab line is still present. Until removed, FEED-22 is not fully live.

### 3. (Optional) Bootstrap `data/active_projects.json` or set `OUTLIER_TRACKING_PROJECT_ID`

**Test:** Either create `data/active_projects.json` with a JSON list of project IDs, or set `OUTLIER_TRACKING_PROJECT_ID` in `.env`.

**Expected:** Step D ("ICP drift") will iterate over real projects instead of short-circuiting with `{"ok": True, "note": "no_active_projects"}` — which would otherwise cause the consolidated message to say "No active projects configured for drift tracking."

**Why human:** Project-list management is operational, not code. Plans 05-08 do not prescribe which projects to track.

## Recommendation

**Status: human_needed**

All FEED-15 through FEED-23 requirements are **code-complete and verified**. The full test suite (48/48) passes, every V2 must-have grep check passes, all v1 contracts are preserved, and cross-plan integration is wired correctly. Vocabulary compliance is clean across all production source paths.

The single open item is operational, not code-level: FEED-22's launchd plist edit and crontab dedup were explicitly carved out of the agent's scope by Plan 08's `<critical_constraints>`. The agent did the right thing by surfacing the user-action commands in `README.md` and the SUMMARY's "USER ACTION REQUIRED" block instead of risking the user's plist setup.

Once the user runs the three documented commands (plist edit + launchctl reload + crontab dedup), FEED-22 will be fully live and Phase 2.5 V2 will be operationally complete. Code-side, Phase 2.5 V2 is done — proceed to whichever next phase the user prioritizes (downstream consumers of `sentiment_callouts.json` or drift trigger reasons, per the SUMMARY's "Next Phase Readiness" notes).
